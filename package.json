{
  "name": "boilerplate-react-auth",
  "version": "1.0.0",
  "description": "boilerplate for client react apps using authO",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "targets": {
    "default": {
      "distDir": "./public"
    }
  },
  "scripts": {
    "prestart": "env MYTEST=%TEST%",
    "start": "parcel --open --port 3030 --dist-dir dev",
    "poststart": "check-env",
    "prebuild": "npm run clear",
    "build": "parcel build",
    "clear": "rm -rf public dev .parcel-cache"
  },
  "keywords": [
    "parcel"
  ],
  "author": "dan moneypenny",
  "license": "MIT",
  "devDependencies": {
    "@auth0/auth0-react": "^1.9.0",
    "dotenv-cli": "^4.1.1",
    "parcel": "^2.2.1"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.1"
  }
}
